import { n as __toESM } from "./rolldown-runtime-FDOR9p9I.js";
import { t as require_react } from "./react.js";
//#region node_modules/use-sound/dist/use-sound.esm.js
var import_react = /* @__PURE__ */ __toESM(require_react());
function _extends() {
	return _extends = Object.assign ? Object.assign.bind() : function(n) {
		for (var e = 1; e < arguments.length; e++) {
			var t = arguments[e];
			for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
		}
		return n;
	}, _extends.apply(null, arguments);
}
function _objectWithoutPropertiesLoose(r, e) {
	if (null == r) return {};
	var t = {};
	for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
		if (-1 !== e.indexOf(n)) continue;
		t[n] = r[n];
	}
	return t;
}
function useOnMount(callback) {
	(0, import_react.useEffect)(callback, []);
}
var _excluded = [
	"id",
	"volume",
	"playbackRate",
	"soundEnabled",
	"interrupt",
	"onload"
];
function useSound(src, _temp) {
	var _ref = _temp === void 0 ? {} : _temp, _ref$volume = _ref.volume, volume = _ref$volume === void 0 ? 1 : _ref$volume, _ref$playbackRate = _ref.playbackRate, playbackRate = _ref$playbackRate === void 0 ? 1 : _ref$playbackRate, _ref$soundEnabled = _ref.soundEnabled, soundEnabled = _ref$soundEnabled === void 0 ? true : _ref$soundEnabled, _ref$interrupt = _ref.interrupt, interrupt = _ref$interrupt === void 0 ? false : _ref$interrupt, onload = _ref.onload, delegated = _objectWithoutPropertiesLoose(_ref, _excluded);
	var HowlConstructor = import_react.useRef(null);
	var isMounted = import_react.useRef(false);
	var _React$useState = import_react.useState(null), duration = _React$useState[0], setDuration = _React$useState[1];
	var _React$useState2 = import_react.useState(null), sound = _React$useState2[0], setSound = _React$useState2[1];
	var handleLoad = function handleLoad() {
		if (typeof onload === "function") onload.call(this);
		if (isMounted.current) setDuration(this.duration() * 1e3);
		setSound(this);
	};
	useOnMount(function() {
		import("./howler-DdyHMo4_.js").then((m) => /* @__PURE__ */ __toESM(m.default)).then(function(mod) {
			if (!isMounted.current) {
				var _mod$Howl;
				HowlConstructor.current = (_mod$Howl = mod.Howl) !== null && _mod$Howl !== void 0 ? _mod$Howl : mod["default"].Howl;
				isMounted.current = true;
				new HowlConstructor.current(_extends({
					src: Array.isArray(src) ? src : [src],
					volume,
					rate: playbackRate,
					onload: handleLoad
				}, delegated));
			}
		});
		return function() {
			isMounted.current = false;
		};
	});
	import_react.useEffect(function() {
		if (HowlConstructor.current && sound) setSound(new HowlConstructor.current(_extends({
			src: Array.isArray(src) ? src : [src],
			volume,
			onload: handleLoad
		}, delegated)));
	}, [JSON.stringify(src)]);
	import_react.useEffect(function() {
		if (sound) {
			sound.volume(volume);
			if (!delegated.sprite) sound.rate(playbackRate);
		}
	}, [
		sound,
		volume,
		playbackRate
	]);
	return [import_react.useCallback(function(options) {
		if (typeof options === "undefined") options = {};
		if (!sound || !soundEnabled && !options.forceSoundEnabled) return;
		if (interrupt) sound.stop();
		if (options.playbackRate) sound.rate(options.playbackRate);
		sound.play(options.id);
	}, [
		sound,
		soundEnabled,
		interrupt
	]), {
		sound,
		stop: import_react.useCallback(function(id) {
			if (!sound) return;
			sound.stop(id);
		}, [sound]),
		pause: import_react.useCallback(function(id) {
			if (!sound) return;
			sound.pause(id);
		}, [sound]),
		duration
	}];
}
//#endregion
export { useSound as default, useSound };
